'use strict'

//Bucle For
//Control structure that repeat a defined numbers of times

let number = 100

for(let i = 1; i <= number; i++) {          // counter 'i', condition, increment o decrement
    console.log('The number is:' + i)       // 
    debugger
}